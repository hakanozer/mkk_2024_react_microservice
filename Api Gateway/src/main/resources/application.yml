spring:
  cloud:
    gateway:
      mvc:
        routes:
          - id: loginModule
            uri: lb://login
            predicates:
              - Path=/customer/**
          - id: productModule
            uri: lb://product
            predicates:
              - Path=/product/**
          - id: basketModule
            uri: lb://basket
            predicates:
              - Path=/basket/**
